# APAR console — five-minute walkthrough for Dylan

Start at `http://127.0.0.1:4173/overview` after running the documented launcher.
Keep the browser at 100% zoom. The left navigation and “continue” footer both
follow the same six-step journey.

## 0:00–0:40 — Overview / Threat

Say: “APAR is an assurance layer for adaptive payment risk. The selected golden
path is an AI-personalized authorized-push-payment scam that converges on a mule
network. The shift is bounded: personalization and message iteration become
faster; we do not claim autonomous settlement access.”

Point to the three rail contexts, then the evidence boundary. Note that FCA and
FBI facts are visually separated from the APAR modeling inference. Briefly
select event **04** in the decision footprint to show that the chart exposes
only the bound probability and action—not post-event truth. Click
**Inspect scenario controls**.

## 0:40–1:15 — Scenario

Say: “This is synthetic-only, deterministic, and read-only. The campaign has a
24-hour envelope, a query budget of 40, event-time ordering, and a conserved
$500 synthetic ledger.”

The displayed campaign generator seed `260816` belongs to this deterministic
network illustration. It is not the portable checkpoint evidence seed. Point
to the fan-in, layer, fan-out, cash-out motif; select the three configured stage
cards to show the ordered emphasis without autoplay, then click **Start verified
replay**.

## 1:15–2:15 — Replay / Intervention

Say: “The console calls the real local scorer first. The green runtime label
shows `ensemble_with_graph`. If the worker fails, the same view explicitly
switches to a hash-bound verified fallback.”

Click **Play both streams** briefly. One presentation transport advances the 10
genuine payment edges and the 12 portable decisions in their respective
repository order. The orange transfer marker moves between the active source
and target while both inspectors update. Say: “The shared control keeps the
walkthrough synchronized; it is not a row-level join between the two streams.”

On the right, point out the independently selected calibrated probability,
bound action thresholds, final action, observed local or fixed-trace latency,
feature count, and repository reason code. Call out the explicit “No
payment-to-trace record mapping asserted” boundary. Emphasize that post-event
truth sits in its own amber examination panel and was withheld from the model.
Use the portable trace dots or event ledger to inspect the A2A APP event without
moving the campaign selector. **Reset both** returns both streams to their
canonical first record. Do not describe the 12 cases as production estimates.

Open **Investigation**.

## 2:15–3:05 — Investigation

Say: “The investigator sees the campaign, not twelve disconnected alerts. This
is a genuine deterministic synthetic graph: 14 entities, 10 payment edges, and
a conserved $500 attempted-value progression.”

Select the central mule node. Point to its linked payments and the single
deterministic case group. The proportional orange bars use only the selected
node's genuine linked payment amounts. Call out **Evidence pending** for analyst
time: the UI does not invent a productivity number.

Open **Defenses**.

## 3:05–4:05 — Defenses

Say: “Four architecture arms are visible, but only one is the demo champion.
The accepted portable arm is the Stage 30 `ensemble_with_graph` model.”

Use **Inspect arm** to compare `rules_only` with `ensemble_with_graph`; the
focused readout exposes the existing description, status, and deterministic
result hash without inventing a new architecture metric.

Point to the exact recovered-evidence qualifier and the two false flags. Say:
“These four-arm metrics are recovered diagnostics. They are non-authoritative,
not accepted capacity evidence, and the official chain is incomplete at Stage
70.”

Then point to the three failed `full_sentinel` gates: false-decline rate,
challenge rate, and benign-only. Conclude: “The graph ensemble is currently
usable for the competition demonstration. Deterministic full-hybrid routing
requires policy refinement.”

Open **Assurance**.

## 4:05–5:00 — Assurance / Integrity

Select two artifact rows to expose their full hashes, then walk down the blocked
human-promotion gate. Say:
“No model can approve itself.”

In the green TrustVerifier section, say: “This separate proof shows genuine
tested identity, mandate, scope, cart-binding, and replay rejection codes. It is
an authorization-integrity claim, not ensemble performance evidence.”

Finish on the seed execution record:

“The portable demo and recovered Kaggle metrics use seed 404 only. No Kaggle
locked-successor or seed-2404 chain ran. An earlier local locked-development
attempt started and irreversibly aborted; it published no candidate manifest,
chunks, judge summary, or successful seed-2404 result, and retry is not
permitted.”

Close with: “This is synthetic evidence before assertion: transparent inputs,
replayable decisions, honest failure gates, and an explicit human promotion
boundary.”

## Presenter cautions

- Never call the live prediction `full_sentinel` or the complete hybrid.
- Never call recovered diagnostics official Stage 70 evidence.
- Never call curated replay metrics production, real-Mastercard-data, or
  external-validity estimates.
- Do not make an absolute non-execution claim about seed 2404; use the exact
  boundary above.
- Do not imply that the TrustVerifier proves fraud-model performance.
